{
  "name": "ig-auto-reply",
  "version": "2.0.0",
  "description": "Instagram comment + DM automation with per-video rules and a web dashboard, via Meta's official API.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test/test.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^4.19.0",
    "dotenv": "^16.4.0"
  }
}
